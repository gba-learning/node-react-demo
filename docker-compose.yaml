services:
  backend:
    image: guillaumeb0/gba-test
    command: tail -f /dev/null
    ports:
      - 8080:3000
    volumes:
      - ./backend:/app/

  terragrunt:
    build:
      context: ./infrastructure/
    volumes:
      - ./infrastructure:/workspace
      - ~/.aws/:/root/.aws
    command: tail -f /dev/null